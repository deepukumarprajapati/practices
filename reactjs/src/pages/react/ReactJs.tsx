import { Box, Typography } from "@mui/material";

const ReactJs = () => {
  return (
    <Box>
      <Typography
        variant="body1"
        sx={{
          mb: 2,
          lineHeight: 1.8,
          textAlign: "justify",
        }}
      >
        React.js is a free, open-source JavaScript library used for building
        user interfaces (UIs), especially for single-page web applications. It
        was developed by Meta Platforms and helps developers create fast,
        interactive, and reusable UI components.
      </Typography>
    </Box>
  );
};

export default ReactJs;
